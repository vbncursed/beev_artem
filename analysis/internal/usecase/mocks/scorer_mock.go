// Code generated by http://github.com/gojuno/minimock (v3.4.7). DO NOT EDIT.

package mocks

import (
	"sync"
	mm_atomic "sync/atomic"
	mm_time "time"

	"github.com/artem13815/hr/analysis/internal/domain"
	"github.com/gojuno/minimock/v3"
)

// ScorerMock implements mm_usecase.Scorer
type ScorerMock struct {
	t          minimock.Tester
	finishOnce sync.Once

	funcScore          func(resumeText string, skills []domain.VacancySkill) (a1 domain.AnalysisPayload)
	funcScoreOrigin    string
	inspectFuncScore   func(resumeText string, skills []domain.VacancySkill)
	afterScoreCounter  uint64
	beforeScoreCounter uint64
	ScoreMock          mScorerMockScore
}

// NewScorerMock returns a mock for mm_usecase.Scorer
func NewScorerMock(t minimock.Tester) *ScorerMock {
	m := &ScorerMock{t: t}

	if controller, ok := t.(minimock.MockController); ok {
		controller.RegisterMocker(m)
	}

	m.ScoreMock = mScorerMockScore{mock: m}
	m.ScoreMock.callArgs = []*ScorerMockScoreParams{}

	t.Cleanup(m.MinimockFinish)

	return m
}

type mScorerMockScore struct {
	optional           bool
	mock               *ScorerMock
	defaultExpectation *ScorerMockScoreExpectation
	expectations       []*ScorerMockScoreExpectation

	callArgs []*ScorerMockScoreParams
	mutex    sync.RWMutex

	expectedInvocations       uint64
	expectedInvocationsOrigin string
}

// ScorerMockScoreExpectation specifies expectation struct of the Scorer.Score
type ScorerMockScoreExpectation struct {
	mock               *ScorerMock
	params             *ScorerMockScoreParams
	paramPtrs          *ScorerMockScoreParamPtrs
	expectationOrigins ScorerMockScoreExpectationOrigins
	results            *ScorerMockScoreResults
	returnOrigin       string
	Counter            uint64
}

// ScorerMockScoreParams contains parameters of the Scorer.Score
type ScorerMockScoreParams struct {
	resumeText string
	skills     []domain.VacancySkill
}

// ScorerMockScoreParamPtrs contains pointers to parameters of the Scorer.Score
type ScorerMockScoreParamPtrs struct {
	resumeText *string
	skills     *[]domain.VacancySkill
}

// ScorerMockScoreResults contains results of the Scorer.Score
type ScorerMockScoreResults struct {
	a1 domain.AnalysisPayload
}

// ScorerMockScoreOrigins contains origins of expectations of the Scorer.Score
type ScorerMockScoreExpectationOrigins struct {
	origin           string
	originResumeText string
	originSkills     string
}

// Marks this method to be optional. The default behavior of any method with Return() is '1 or more', meaning
// the test will fail minimock's automatic final call check if the mocked method was not called at least once.
// Optional() makes method check to work in '0 or more' mode.
// It is NOT RECOMMENDED to use this option unless you really need it, as default behaviour helps to
// catch the problems when the expected method call is totally skipped during test run.
func (mmScore *mScorerMockScore) Optional() *mScorerMockScore {
	mmScore.optional = true
	return mmScore
}

// Expect sets up expected params for Scorer.Score
func (mmScore *mScorerMockScore) Expect(resumeText string, skills []domain.VacancySkill) *mScorerMockScore {
	if mmScore.mock.funcScore != nil {
		mmScore.mock.t.Fatalf("ScorerMock.Score mock is already set by Set")
	}

	if mmScore.defaultExpectation == nil {
		mmScore.defaultExpectation = &ScorerMockScoreExpectation{}
	}

	if mmScore.defaultExpectation.paramPtrs != nil {
		mmScore.mock.t.Fatalf("ScorerMock.Score mock is already set by ExpectParams functions")
	}

	mmScore.defaultExpectation.params = &ScorerMockScoreParams{resumeText, skills}
	mmScore.defaultExpectation.expectationOrigins.origin = minimock.CallerInfo(1)
	for _, e := range mmScore.expectations {
		if minimock.Equal(e.params, mmScore.defaultExpectation.params) {
			mmScore.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmScore.defaultExpectation.params)
		}
	}

	return mmScore
}

// ExpectResumeTextParam1 sets up expected param resumeText for Scorer.Score
func (mmScore *mScorerMockScore) ExpectResumeTextParam1(resumeText string) *mScorerMockScore {
	if mmScore.mock.funcScore != nil {
		mmScore.mock.t.Fatalf("ScorerMock.Score mock is already set by Set")
	}

	if mmScore.defaultExpectation == nil {
		mmScore.defaultExpectation = &ScorerMockScoreExpectation{}
	}

	if mmScore.defaultExpectation.params != nil {
		mmScore.mock.t.Fatalf("ScorerMock.Score mock is already set by Expect")
	}

	if mmScore.defaultExpectation.paramPtrs == nil {
		mmScore.defaultExpectation.paramPtrs = &ScorerMockScoreParamPtrs{}
	}
	mmScore.defaultExpectation.paramPtrs.resumeText = &resumeText
	mmScore.defaultExpectation.expectationOrigins.originResumeText = minimock.CallerInfo(1)

	return mmScore
}

// ExpectSkillsParam2 sets up expected param skills for Scorer.Score
func (mmScore *mScorerMockScore) ExpectSkillsParam2(skills []domain.VacancySkill) *mScorerMockScore {
	if mmScore.mock.funcScore != nil {
		mmScore.mock.t.Fatalf("ScorerMock.Score mock is already set by Set")
	}

	if mmScore.defaultExpectation == nil {
		mmScore.defaultExpectation = &ScorerMockScoreExpectation{}
	}

	if mmScore.defaultExpectation.params != nil {
		mmScore.mock.t.Fatalf("ScorerMock.Score mock is already set by Expect")
	}

	if mmScore.defaultExpectation.paramPtrs == nil {
		mmScore.defaultExpectation.paramPtrs = &ScorerMockScoreParamPtrs{}
	}
	mmScore.defaultExpectation.paramPtrs.skills = &skills
	mmScore.defaultExpectation.expectationOrigins.originSkills = minimock.CallerInfo(1)

	return mmScore
}

// Inspect accepts an inspector function that has same arguments as the Scorer.Score
func (mmScore *mScorerMockScore) Inspect(f func(resumeText string, skills []domain.VacancySkill)) *mScorerMockScore {
	if mmScore.mock.inspectFuncScore != nil {
		mmScore.mock.t.Fatalf("Inspect function is already set for ScorerMock.Score")
	}

	mmScore.mock.inspectFuncScore = f

	return mmScore
}

// Return sets up results that will be returned by Scorer.Score
func (mmScore *mScorerMockScore) Return(a1 domain.AnalysisPayload) *ScorerMock {
	if mmScore.mock.funcScore != nil {
		mmScore.mock.t.Fatalf("ScorerMock.Score mock is already set by Set")
	}

	if mmScore.defaultExpectation == nil {
		mmScore.defaultExpectation = &ScorerMockScoreExpectation{mock: mmScore.mock}
	}
	mmScore.defaultExpectation.results = &ScorerMockScoreResults{a1}
	mmScore.defaultExpectation.returnOrigin = minimock.CallerInfo(1)
	return mmScore.mock
}

// Set uses given function f to mock the Scorer.Score method
func (mmScore *mScorerMockScore) Set(f func(resumeText string, skills []domain.VacancySkill) (a1 domain.AnalysisPayload)) *ScorerMock {
	if mmScore.defaultExpectation != nil {
		mmScore.mock.t.Fatalf("Default expectation is already set for the Scorer.Score method")
	}

	if len(mmScore.expectations) > 0 {
		mmScore.mock.t.Fatalf("Some expectations are already set for the Scorer.Score method")
	}

	mmScore.mock.funcScore = f
	mmScore.mock.funcScoreOrigin = minimock.CallerInfo(1)
	return mmScore.mock
}

// When sets expectation for the Scorer.Score which will trigger the result defined by the following
// Then helper
func (mmScore *mScorerMockScore) When(resumeText string, skills []domain.VacancySkill) *ScorerMockScoreExpectation {
	if mmScore.mock.funcScore != nil {
		mmScore.mock.t.Fatalf("ScorerMock.Score mock is already set by Set")
	}

	expectation := &ScorerMockScoreExpectation{
		mock:               mmScore.mock,
		params:             &ScorerMockScoreParams{resumeText, skills},
		expectationOrigins: ScorerMockScoreExpectationOrigins{origin: minimock.CallerInfo(1)},
	}
	mmScore.expectations = append(mmScore.expectations, expectation)
	return expectation
}

// Then sets up Scorer.Score return parameters for the expectation previously defined by the When method
func (e *ScorerMockScoreExpectation) Then(a1 domain.AnalysisPayload) *ScorerMock {
	e.results = &ScorerMockScoreResults{a1}
	return e.mock
}

// Times sets number of times Scorer.Score should be invoked
func (mmScore *mScorerMockScore) Times(n uint64) *mScorerMockScore {
	if n == 0 {
		mmScore.mock.t.Fatalf("Times of ScorerMock.Score mock can not be zero")
	}
	mm_atomic.StoreUint64(&mmScore.expectedInvocations, n)
	mmScore.expectedInvocationsOrigin = minimock.CallerInfo(1)
	return mmScore
}

func (mmScore *mScorerMockScore) invocationsDone() bool {
	if len(mmScore.expectations) == 0 && mmScore.defaultExpectation == nil && mmScore.mock.funcScore == nil {
		return true
	}

	totalInvocations := mm_atomic.LoadUint64(&mmScore.mock.afterScoreCounter)
	expectedInvocations := mm_atomic.LoadUint64(&mmScore.expectedInvocations)

	return totalInvocations > 0 && (expectedInvocations == 0 || expectedInvocations == totalInvocations)
}

// Score implements mm_usecase.Scorer
func (mmScore *ScorerMock) Score(resumeText string, skills []domain.VacancySkill) (a1 domain.AnalysisPayload) {
	mm_atomic.AddUint64(&mmScore.beforeScoreCounter, 1)
	defer mm_atomic.AddUint64(&mmScore.afterScoreCounter, 1)

	mmScore.t.Helper()

	if mmScore.inspectFuncScore != nil {
		mmScore.inspectFuncScore(resumeText, skills)
	}

	mm_params := ScorerMockScoreParams{resumeText, skills}

	// Record call args
	mmScore.ScoreMock.mutex.Lock()
	mmScore.ScoreMock.callArgs = append(mmScore.ScoreMock.callArgs, &mm_params)
	mmScore.ScoreMock.mutex.Unlock()

	for _, e := range mmScore.ScoreMock.expectations {
		if minimock.Equal(*e.params, mm_params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return e.results.a1
		}
	}

	if mmScore.ScoreMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmScore.ScoreMock.defaultExpectation.Counter, 1)
		mm_want := mmScore.ScoreMock.defaultExpectation.params
		mm_want_ptrs := mmScore.ScoreMock.defaultExpectation.paramPtrs

		mm_got := ScorerMockScoreParams{resumeText, skills}

		if mm_want_ptrs != nil {

			if mm_want_ptrs.resumeText != nil && !minimock.Equal(*mm_want_ptrs.resumeText, mm_got.resumeText) {
				mmScore.t.Errorf("ScorerMock.Score got unexpected parameter resumeText, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
					mmScore.ScoreMock.defaultExpectation.expectationOrigins.originResumeText, *mm_want_ptrs.resumeText, mm_got.resumeText, minimock.Diff(*mm_want_ptrs.resumeText, mm_got.resumeText))
			}

			if mm_want_ptrs.skills != nil && !minimock.Equal(*mm_want_ptrs.skills, mm_got.skills) {
				mmScore.t.Errorf("ScorerMock.Score got unexpected parameter skills, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
					mmScore.ScoreMock.defaultExpectation.expectationOrigins.originSkills, *mm_want_ptrs.skills, mm_got.skills, minimock.Diff(*mm_want_ptrs.skills, mm_got.skills))
			}

		} else if mm_want != nil && !minimock.Equal(*mm_want, mm_got) {
			mmScore.t.Errorf("ScorerMock.Score got unexpected parameters, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
				mmScore.ScoreMock.defaultExpectation.expectationOrigins.origin, *mm_want, mm_got, minimock.Diff(*mm_want, mm_got))
		}

		mm_results := mmScore.ScoreMock.defaultExpectation.results
		if mm_results == nil {
			mmScore.t.Fatal("No results are set for the ScorerMock.Score")
		}
		return (*mm_results).a1
	}
	if mmScore.funcScore != nil {
		return mmScore.funcScore(resumeText, skills)
	}
	mmScore.t.Fatalf("Unexpected call to ScorerMock.Score. %v %v", resumeText, skills)
	return
}

// ScoreAfterCounter returns a count of finished ScorerMock.Score invocations
func (mmScore *ScorerMock) ScoreAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmScore.afterScoreCounter)
}

// ScoreBeforeCounter returns a count of ScorerMock.Score invocations
func (mmScore *ScorerMock) ScoreBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmScore.beforeScoreCounter)
}

// Calls returns a list of arguments used in each call to ScorerMock.Score.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmScore *mScorerMockScore) Calls() []*ScorerMockScoreParams {
	mmScore.mutex.RLock()

	argCopy := make([]*ScorerMockScoreParams, len(mmScore.callArgs))
	copy(argCopy, mmScore.callArgs)

	mmScore.mutex.RUnlock()

	return argCopy
}

// MinimockScoreDone returns true if the count of the Score invocations corresponds
// the number of defined expectations
func (m *ScorerMock) MinimockScoreDone() bool {
	if m.ScoreMock.optional {
		// Optional methods provide '0 or more' call count restriction.
		return true
	}

	for _, e := range m.ScoreMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	return m.ScoreMock.invocationsDone()
}

// MinimockScoreInspect logs each unmet expectation
func (m *ScorerMock) MinimockScoreInspect() {
	for _, e := range m.ScoreMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to ScorerMock.Score at\n%s with params: %#v", e.expectationOrigins.origin, *e.params)
		}
	}

	afterScoreCounter := mm_atomic.LoadUint64(&m.afterScoreCounter)
	// if default expectation was set then invocations count should be greater than zero
	if m.ScoreMock.defaultExpectation != nil && afterScoreCounter < 1 {
		if m.ScoreMock.defaultExpectation.params == nil {
			m.t.Errorf("Expected call to ScorerMock.Score at\n%s", m.ScoreMock.defaultExpectation.returnOrigin)
		} else {
			m.t.Errorf("Expected call to ScorerMock.Score at\n%s with params: %#v", m.ScoreMock.defaultExpectation.expectationOrigins.origin, *m.ScoreMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcScore != nil && afterScoreCounter < 1 {
		m.t.Errorf("Expected call to ScorerMock.Score at\n%s", m.funcScoreOrigin)
	}

	if !m.ScoreMock.invocationsDone() && afterScoreCounter > 0 {
		m.t.Errorf("Expected %d calls to ScorerMock.Score at\n%s but found %d calls",
			mm_atomic.LoadUint64(&m.ScoreMock.expectedInvocations), m.ScoreMock.expectedInvocationsOrigin, afterScoreCounter)
	}
}

// MinimockFinish checks that all mocked methods have been called the expected number of times
func (m *ScorerMock) MinimockFinish() {
	m.finishOnce.Do(func() {
		if !m.minimockDone() {
			m.MinimockScoreInspect()
		}
	})
}

// MinimockWait waits for all mocked methods to be called the expected number of times
func (m *ScorerMock) MinimockWait(timeout mm_time.Duration) {
	timeoutCh := mm_time.After(timeout)
	for {
		if m.minimockDone() {
			return
		}
		select {
		case <-timeoutCh:
			m.MinimockFinish()
			return
		case <-mm_time.After(10 * mm_time.Millisecond):
		}
	}
}

func (m *ScorerMock) minimockDone() bool {
	done := true
	return done &&
		m.MinimockScoreDone()
}

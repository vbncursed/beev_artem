# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Admin API
    description: |-
        AdminService is the operational dashboard contract — admin-only by
         transport interceptor. Every RPC requires a JWT with role=admin;
         non-admin callers see Unauthenticated/PermissionDenied.
    version: 1.0.0
paths:
    /api/v1/admin/overview:
        get:
            tags:
                - AdminService
            description: GetOverview returns aggregated platform statistics.
            operationId: AdminService_GetOverview
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OverviewResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/admin/users:
        get:
            tags:
                - AdminService
            description: |-
                ListUsers enumerates every HR account with their role and activity
                 counters (vacancies owned, candidates uploaded).
            operationId: AdminService_ListUsers
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListUsersResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/admin/users/{userId}/demote:
        post:
            tags:
                - AdminService
            description: DemoteUser sets role = "user".
            operationId: AdminService_DemoteUser
            parameters:
                - name: userId
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DemoteUserRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateRoleResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/admin/users/{userId}/promote:
        post:
            tags:
                - AdminService
            description: PromoteUser sets role = "admin" via the auth service.
            operationId: AdminService_PromoteUser
            parameters:
                - name: userId
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/PromoteUserRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateRoleResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        AdminUserView:
            type: object
            properties:
                id:
                    type: string
                email:
                    type: string
                role:
                    type: string
                createdAt:
                    type: string
                    format: date-time
                vacanciesOwned:
                    type: string
                candidatesUploaded:
                    type: string
            description: |-
                AdminUserView mirrors auth's User but adds activity metrics that
                 require cross-table joins (vacancies owned, candidates uploaded).
        DemoteUserRequest:
            type: object
            properties:
                userId:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListUsersResponse:
            type: object
            properties:
                users:
                    type: array
                    items:
                        $ref: '#/components/schemas/AdminUserView'
        OverviewResponse:
            type: object
            properties:
                stats:
                    $ref: '#/components/schemas/SystemStats'
        PromoteUserRequest:
            type: object
            properties:
                userId:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        SystemStats:
            type: object
            properties:
                usersTotal:
                    type: string
                adminsTotal:
                    type: string
                vacanciesTotal:
                    type: string
                candidatesTotal:
                    type: string
                analysesTotal:
                    type: string
                analysesDone:
                    type: string
                analysesFailed:
                    type: string
            description: |-
                SystemStats is the dashboard headline — total counts across the
                 platform. Computed via SELECT COUNT(*) per table; cheap on a 1-instance
                 shared DB, would need batch aggregation if services move to per-DB.
        UpdateRoleResponse:
            type: object
            properties:
                userId:
                    type: string
                newRole:
                    type: string
tags:
    - name: AdminService

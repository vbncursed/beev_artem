server:
  http_addr: ":8080"
  cors:
    # CHANGE_ME — explicit allowlist of frontend origins. Leave empty to
    # disable CORS entirely. Do NOT use ["*"] in production.
    allowed_origins: []

auth:
  grpc_addr: "auth:50050"

vacancy:
  grpc_addr: "vacancy:50051"

resume:
  grpc_addr: "resume:50052"

analysis:
  grpc_addr: "analysis:50054"
